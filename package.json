{
  "name": "clipfast",
  "version": "0.1.0",
  "description": "速贴（ClipFast）桌面端 Electron 应用（V1 核心功能验证）",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "icons": "node scripts/gen-icons.js",
    "dist": "npm run icons && electron-builder --mac",
    "dist:win": "npm run icons && electron-builder --win zip",
    "dist:win:x64": "npm run icons && electron-builder --win zip --x64"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "electron-store": "^9.0.0"
  },
  "devDependencies": {
    "electron": "^31.3.0",
    "electron-builder": "^24.13.1",
    "png2icons": "^2.0.1"
  },
  "build": {
    "appId": "com.clipfast.app",
    "productName": "clipFast",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "src/**",
      "renderer/**",
      "assets/**",
      "package.json"
    ],
    "mac": {
      "category": "public.app-category.productivity",
      "icon": "assets/icon.icns",
      "target": [
        {
          "target": "dmg"
        }
      ]
    },
    "win": {
      "icon": "assets/icon.ico",
      "target": [
        {
          "target": "zip"
        }
      ],
      "artifactName": "ClipFast-${version}-win-${arch}.zip"
    }
  }
}
